<select xmlns:py="http://genshi.edgewall.org/"
        py:strip="strip"
        name="${name}">

    <option py:for="j in jurisdictions"
            value="${j.id}">
        ${j.title(lang)}
    </option>

</select>
